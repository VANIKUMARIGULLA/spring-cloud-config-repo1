server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/VANIKUMARIGULLA/Config-Repo
          default-label: main
          search-paths:
            - .
  profiles:
    active: native

logging:
  level:
    root: INFO
